# KalshiPaperSim

A **paper-trading competition platform for Kalshi prediction markets**, built on real captured
Kalshi data: real markets, real quotes, real order-book depth, real candlesticks, the real
quadratic fee formula, and the real reciprocal YES/NO pricing rule. Forty-one aggressive
algorithmic strategies with unique usernames compete across three flights (daily, hourly, micro);
every result is **computed by a deterministic replay engine** and every input is traceable to an
official source. Roster entries were recreated from public trading write-ups — see
[Strategy sources](#strategy-sources), and the [signal-source ledger](#signal-sources-the-mastersite-review)
for the site-by-site review of the owner's own projects (weather → `KXHIGHNY`, gold → `KXGOLD15M`).

**What is new in this revision (2026-09-18):** the **Live Desk** — a tenth section that places
paper orders on **real, still-open** Kalshi contracts at a chosen cut-off, priced only from
order-book ladders captured from the official API, with depth-bound fills, the published fee
schedule with the series multiplier, real $1.00/$0.00 settlement and a 13-invariant audit
(`node scripts/run-live-desk.mjs`). The universe also gained its first **daily-settling
markets** — 40 real NYC high-temperature brackets (`KXHIGHNY`, 60-minute bars, 39 already finalized
with the exchange's own result) and 8 real 15-minute gold markets (`KXGOLD15M`, 1-minute bars, all
finalized) — so the engine now books **real $1.00/$0.00 settlements mid-replay** instead of marking
to the last quote, a **point-in-time NWS forecast archive** backs the first genuinely forward-tested
strategy (`ForecastEdge_Weather`), and strategies can never see a market's result before it settles
(redaction, enforced by test).

<!-- AUTO:COUNTS-START (regenerated by scripts/render-docs.js — do not edit) -->
99 verified facts (34 documented, 39 captured from production, 1 negative/404 evidence, 20 derived by arithmetic, 5 unexplained observations) · 45 flagged irregularities · 41 strategies · 9 real markets · 10,949 captured daily candlesticks across 53 replayable markets (2025-08-15 → 2026-09-19, 10,813 added by the daily ingest) · 112 automated tests · 13 Live Desk entrants on 63 real open contracts with captured ladders (23 paper fills, $389.5129 official fees, 13-point desk audit passing)
<!-- AUTO:COUNTS-END -->

**Live:** [GitHub Pages build](https://buffedlizard55-lab.github.io/KalshiPaperSim/) (static, computes in your browser) ·
**Audit:** [VERIFICATION.md](VERIFICATION.md) · [IRREGULARITIES.md](IRREGULARITIES.md)

---

## The honesty contract

This repository was rebuilt after an audit found that its predecessor **invented data**. The rules
below are enforced by code and by tests, not by intention.

| Rule | How it is enforced |
| --- | --- |
| No price, quote, fee, market or series is invented | Every value lives in `src/verified-snapshot.js` / `src/verified-candles.js` / `data/history/*.json` with the URL it was captured from |
| No performance number is hard-coded | `validateStrategies()` rejects any strategy carrying a literal result; strategies expose executable `decide(ctx)` code only |
| An oversized order is never filled at a made-up price | `exhaustionPolicy: 'partial'` fills only depth that exists and reports the remainder as **unfilled** |
| No order may fill more than the market actually traded | `maxFillFractionOfPeriodVolume` (default 10%) caps every fill — taker and resting maker — at that share of the bar's real `volume_fp`; the rest is reported, not filled (Irregularity #29) |
| A longer stored series can never rewrite a captured one | `src/history-merge.js` admits a stored series only as a **verified superset** — every shared bar must match field-for-field and the stored one must add a bar |
| Modelled data is labelled as modelled | `depthModel` / `source` on every book; the pill in the UI says REAL SNAPSHOT or SIMULATED |
| A strategy that never traded is not ranked | `LEADERBOARD_QUALIFICATION.minTrades = 1`; it is listed as *unranked* with the reason |
| Attribution must add up | `equityChange = realizedPnl + unrealizedPnl − feesPaid`, asserted per strategy (residual < $0.01) |
| Deep comparisons must be deep | `stableStringify()` — `JSON.stringify(v, keyArray)` filters nested objects and silently skipped every price field (Irregularity #20) |
| Anything odd found in official data is flagged, not smoothed over | 45 entries in [IRREGULARITIES.md](IRREGULARITIES.md) |

What is **real**: market objects, series fee configs, order-book levels, **10,905 captured daily
candlesticks across 53 markets (2025-08-15 → 2026-09-18)**, every bar's traded
volume, exchange status, the historical cutoff, the fee formula and its published table.

What is **modelled** (and labelled): the market-maker feed that moves prices when no API credentials
are configured, and the one-year calendar clock. Depth *behind* the quoted touch is no longer a model
for the tracked universe: `src/captured-depth.js` holds a real ladder captured from
`GET /markets/{ticker}/orderbook` for every one of the 30 markets, and the default run fills against
those ladders re-anchored to each bar's real quoted touch (switch to *Modelled ladder* in the
leaderboard controls to see the comparison — it moves results by up to ~13pp). Fills are *additionally*
bounded by the volume that really traded, and any market without a capture says so per market.

---

## Current computed results

<!-- AUTO:RESULTS-START (regenerated by scripts/render-docs.js — do not edit) -->
Seed `20260917` · 400 real daily candlestick periods across 53 markets (2025-08-15 → 2026-09-19) · $100,000.00 starting capital per entry · fills bounded to 10% of each bar's real traded volume · `exhaustionPolicy: "partial"` (no execution price is ever invented).
Reproduce with `curl -X POST localhost:3000/api/run-competition -d '{"seed":20260917}'` — the run is deterministic.

| # | Username | Strategy | Return | Final equity | Trades | Win rate | Max DD | Fees paid | Contracts NOT filled |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **AdjacentStrike_Ladder** | Adjacent-Strike Cheap Bracket Ladder | +13.50% | $113,496.94 | 2016 | 50.0% | 19.92% | $356.50 | 21,628.47 |
| 2 | **MeanRev_CheapBand** | Cheap-Band Mean Reversion (enter ≤ 0.10, exit ≥ 0.50) | +4.24% | $104,239.32 | 2075 | 100.0% | 33.35% | $476.13 | 28,244.03 |
| 3 | **TightScalp_Fixed5** | Tight Scalp — the family's best setting, promoted as a control | +4.18% | $104,182.91 | 3554 | 99.9% | 7.82% | $127.05 | 0 |
| 4 | **PanicDip_ShockTiming** | Shock-Timing Panic-Dip Buyer (maker ladder) | +1.73% | $101,726.15 | 1504 | 99.9% | 0.86% | $83.28 | 0 |
| 5 | **VolatilityArb_MM** | Two-Sided Spread Harvester (Market Maker) | +1.33% | $101,332.76 | 7023 | 60.1% | 27.85% | $486.72 | 0 |
| 6 | **FeeArb_PremiumBuyer** | Fee-Optimal Favourite Buyer | +1.30% | $101,296.03 | 1054 | 50.0% | 5.28% | $833.31 | 0 |
| 7 | **MakerFlip_SpreadHarvest** | Maker Flip — rest the bid, flip the fill to the offer | +0.43% | $100,433.48 | 2881 | 99.9% | 4.58% | $88.12 | 0 |
| 8 | **YieldVulture_Arb** | Late-Stage High-Probability Sweep | +0.35% | $100,347.58 | 118 | 100.0% | 0.22% | $19.38 | 0 |
| 9 | **MEE_BoardSum** | Mutually-Exclusive-Event Board Sum (R14 `mee`) | +0.33% | $100,327.69 | 8 | 0.0% | 0.41% | $0.00 | 0 |
| 10 | **LongshotFader_FLB** | Favorite–Longshot Bias Fader | +0.11% | $100,107.31 | 51 | 83.3% | 0.22% | $18.48 | 0 |
| 11 | **ShockTiming_ModerateFav** | Shock Timing — the source's favourite-bucket filter, as a measured control | +0.00% | $100,000.03 | 6 | 100.0% | 0.00% | $0.01 | 0 |
| 12 | **FDALadder_Dominance** | FDA Cumulative-Ladder Dominance | -0.01% | $99,986.02 | 2 | 0.0% | 0.03% | $0.74 | 0 |
| 13 | **CEOExit_Drift** | CEO-Exit News Drift | -0.14% | $99,859.01 | 2 | 100.0% | 0.20% | $10.24 | 0 |
| 14 | **FedPivotSniper** | Macro Print Mean-Reversion Sniper | -0.37% | $99,628.82 | 846 | 31.4% | 0.57% | $244.87 | 0 |
| 15 | **BookWall_BidLadder** | Order-Book Wall Maker Ladder | -0.38% | $99,617.19 | 1759 | 99.9% | 6.81% | $38.93 | 0 |
| 16 | **PanicFade_HourlyVol** | Hourly Panic-Fade Volatility Reversion | -0.49% | $99,505.25 | 596 | 99.7% | 1.07% | $343.22 | 0 |
| 17 | **ContrarianKing_100x** | Deep-OTM Longshot Convexity Hunter | -0.56% | $99,443.70 | 3482 | 50.0% | 29.59% | $941.83 | 31,982.3 |
| 18 | **PanicFadeDeep_T6_TP6** | Deep Panic Fade with a 6-cent Take-Profit | -0.84% | $99,156.23 | 509 | 96.6% | 1.27% | $280.04 | 0 |
| 19 | **ThetaHarvest_ShortOTM** | Deep-OTM Decay Harvester (buys NO) | -1.90% | $98,095.96 | 477 | 50.0% | 3.49% | $159.63 | 509.93 |
| 20 | **PanicFade_T4_S100** | Panic Fade — the source's own best configuration | -3.09% | $96,910.00 | 426 | 80.0% | 4.48% | $456.71 | 0 |
| 21 | **ShockTiming_StopOut** | Shock-Timing with the Source's Stop-Out | -3.79% | $96,214.70 | 1002 | 32.8% | 5.37% | $876.46 | 1,363.17 |
| 22 | **TrendRide_FullTilt** | Trend-Following Pyramider | -5.73% | $94,267.42 | 632 | 75.0% | 12.30% | $693.54 | 15,336.97 |
| 23 | **EventCatalyst_Max** | Range-Breakout Catalyst Frontrunner | -9.24% | $90,757.97 | 1034 | 80.0% | 14.20% | $1,167.38 | 7,858.28 |
| 24 | **AlphaApex_Momentum** | Maximum Convexity Momentum Chaser | -12.87% | $87,125.95 | 2480 | 75.0% | 32.50% | $1,980.60 | 30,212.43 |
| 25 | **GammaWhale_Squeeze** | High-Gamma Volume-Spike Breakout | -17.90% | $82,096.99 | 820 | 80.0% | 23.59% | $2,126.70 | 47,435.35 |
| — | **CPI_PrintFade** | CPI-Print Panic Fade | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **Crypto15M_CheapTail** | 15-Minute Crypto Cheap-Tail Buyer | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **Crypto15M_EarlyLeader** | 15-Minute Crypto Early-Leader Ride (BTC · ETH · SOL) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **FadeSpike_Micro** | 5-Minute Spike Fade on 1-Minute Bars (R14 `fade`) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **FedBucket_Ladder** | Fed-Decision Bucket Ladder | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **ForecastEdge_MultiCity** | Multi-City Point-in-Time Forecast Confirmation | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **ForecastEdge_Weather** | Point-in-Time Forecast-Divergence Buyer | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **GoldBracket_EarlyLeader** | 15-Minute Gold Early-Leader Ride | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **MomTick_Micro** | 1-Minute Momentum Follow (R14 `mom`, adapted) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **SportsFavourite_Settle** | Game-Line Favourite, Held to Settlement | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **SportsLine_Momentum** | In-Play Line-Move Follower | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **SportsSteam_Fade** | Steam Fader (buys the side the market just dumped) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **SportsUnderdog_Sweep** | Cheap-Underdog Convexity Sweep | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **WeatherFavourite_Decay** | Weather Favourite Longshot-Fader (buys the likely band, sells the tail) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **WeatherLadder_CheapBands** | Cheap Weather-Bracket Ladder (settles for real) | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |
| — | **WeatherLadder_MultiCity** | Multi-City Cheap Weather-Bracket Ladder | *unranked* | $100,000.00 | 0 | — | — | $0.00 | 0 |

> **CPI_PrintFade is not ranked** — Flight mismatch — no KXCPIYOY market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **Crypto15M_CheapTail is not ranked** — Flight mismatch — no KXBTC15M / KXETH15M / KXSOL15M market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **Crypto15M_EarlyLeader is not ranked** — Flight mismatch — no KXBTC15M / KXETH15M / KXSOL15M market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **FadeSpike_Micro is not ranked** — Flight mismatch — no KXBTC15M / KXETH15M / KXSOL15M / KXGOLD15M market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **FedBucket_Ladder is not ranked** — Flight mismatch — no KXFEDDECISION market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **ForecastEdge_MultiCity is not ranked** — Flight mismatch — no KXHIGHNY / KXHIGHLAX / KXHIGHCHI / KXHIGHMIA / KXHIGHAUS / KXHIGHDEN / KXHIGHPHIL / KXHIGHTPHX / KXHIGHTSEA market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **ForecastEdge_Weather is not ranked** — Flight mismatch — no KXHIGHNY market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **GoldBracket_EarlyLeader is not ranked** — Flight mismatch — no KXGOLD15M market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **MomTick_Micro is not ranked** — Flight mismatch — no KXBTC15M / KXETH15M / KXSOL15M / KXGOLD15M market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **SportsFavourite_Settle is not ranked** — Flight mismatch — no KXNFLGAME / KXMLBGAME / KXNBAGAME / KXNHLGAME / KXNCAAFGAME / KXWNBAGAME / KXUFCFIGHT market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **SportsLine_Momentum is not ranked** — Flight mismatch — no KXNFLGAME / KXMLBGAME / KXNBAGAME / KXNHLGAME / KXNCAAFGAME / KXWNBAGAME / KXUFCFIGHT market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **SportsSteam_Fade is not ranked** — Flight mismatch — no KXNFLGAME / KXMLBGAME / KXNBAGAME / KXNHLGAME / KXNCAAFGAME / KXWNBAGAME / KXUFCFIGHT market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **SportsUnderdog_Sweep is not ranked** — Flight mismatch — no KXNFLGAME / KXMLBGAME / KXNBAGAME / KXNHLGAME / KXNCAAFGAME / KXWNBAGAME / KXUFCFIGHT market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **WeatherFavourite_Decay is not ranked** — Flight mismatch — no KXHIGHNY / KXHIGHLAX / KXHIGHCHI / KXHIGHMIA / KXHIGHAUS / KXHIGHDEN / KXHIGHPHIL / KXHIGHTPHX / KXHIGHTSEA market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **WeatherLadder_CheapBands is not ranked** — Flight mismatch — no KXHIGHNY market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.
> **WeatherLadder_MultiCity is not ranked** — Flight mismatch — no KXHIGHNY / KXHIGHLAX / KXHIGHCHI / KXHIGHMIA / KXHIGHAUS / KXHIGHDEN / KXHIGHPHIL / KXHIGHTPHX / KXHIGHTSEA market in this flight's universe — this design cannot trade here (this design runs in another flight.). Listing it at "0%" would present a design that never traded as if it were a competitive result, so `LEADERBOARD_QUALIFICATION` excludes it from ranking entirely.

**How to read this table.** 10 of the 25 ranked entries finished ahead of their $100,000 starting capital —
best +13.50% (AdjacentStrike_Ladder), worst -17.90%.
There is no +800% curve here, and that is the honest outcome of replaying 400 daily periods across
53 real markets whose YES contracts traded between $0.06 and $0.28,
under Kalshi's real quadratic fee schedule. Two things separate the top of this table from the bottom:
the winners either trade on **resting maker orders** (which pay a quarter of the taker fee) or **buy the cheap side of the fee curve**,
while the losers pay taker fees on huge notional to hold deep out-of-the-money convexity that never resolved in the window.
The "Contracts NOT filled" column tells the other half of the story — aggressive sizing hits the wall of real liquidity, and the
engine reports the missed volume instead of inventing an execution price for it.
Every number here is recomputed on demand; nothing in this table is stored.

### The intraday flights (hourly 60m · micro 1m) — reported separately, never merged

`flights.json` · The hourly flight replays the 60-minute store; the micro flight replays the 1-minute store. Both paragraphs below are **computed from the store at build time** (`flightCaption` in `src/store-facts.js`), so a market that settles or a bar that lands changes the sentence. A strategy that needs intraday bars cannot be judged on daily ones, so these are separate leaderboards.

**HOURLY STORE** — 207 market(s) across 20 series, 174 of them finalized with the exchange's own result (12,748 numeric closes spanning $0.00–$0.99); largest: KXHIGHNY (41 markets, 40 settled, 1,622 bars); KXHIGHTSEA (13 markets, 12 settled, 424 bars); KXHIGHPHIL (13 markets, 12 settled, 376 bars)

**MICRO STORE** — 59 market(s) across 4 series, 59 of them finalized with the exchange's own result (885 numeric closes spanning $0.00–$1.00); largest: KXETH15M (16 markets, 16 settled, 256 bars); KXGOLD15M (16 markets, 16 settled, 256 bars); KXBTC15M (15 markets, 15 settled, 240 bars)

**HOURLY (period_interval 60)** — 1485 hourly periods across 190 markets

| # | Username | Return | Trades | Real settlements booked | Real payout |
| --- | --- | --- | --- | --- | --- |
| 1 | **PanicFade_HourlyVol** | +62.77% | 1160 | 99 | $177,877.36 |
| 2 | **SportsUnderdog_Sweep** | +42.68% | 11 | 10 | $72,197.05 |
| 3 | **SportsLine_Momentum** | +28.98% | 18 | 10 | $126,897.00 |
| 4 | **SportsSteam_Fade** | +3.09% | 18 | 15 | $73,671.32 |
| 5 | **MEE_BoardSum** | +1.18% | 14 | 11 | $1,659.29 |
| 6 | **WeatherLadder_MultiCity** | +0.13% | 43 | 38 | $161.97 |
| 7 | **WeatherLadder_CheapBands** | -0.01% | 6 | 5 | $0.00 |
| 8 | **ForecastEdge_Weather** | -0.09% | 1 | 0 | $0.00 |
| 9 | **SportsFavourite_Settle** | -0.12% | 18 | 13 | $169.43 |
| 10 | **ForecastEdge_MultiCity** | -0.13% | 3 | 0 | $0.00 |
| 11 | **WeatherFavourite_Decay** | -2.09% | 58 | 57 | $20,141.34 |
| 12 | **FedBucket_Ladder** | -4.34% | 6 | 6 | $0.00 |
| 13 | **VolatilityArb_MM** | -92.94% | 6969 | 149 | $220,505.26 |
| 14 | **MakerFlip_SpreadHarvest** | -93.86% | 3811 | 124 | $167,916.16 |
| 15 | **PanicDip_ShockTiming** | -99.96% | 1176 | 84 | $75,016.41 |
| — | **CPI_PrintFade** *(unranked)* | *no trades* | 0 | 0 | $0.00 |

**MICRO (period_interval 1)** — 699 one-minute periods across 59 markets

| # | Username | Return | Trades | Real settlements booked | Real payout |
| --- | --- | --- | --- | --- | --- |
| 1 | **FadeSpike_Micro** | +87.29% | 59 | 59 | $179,748.96 |
| 2 | **Crypto15M_CheapTail** | +33.92% | 43 | 43 | $54,752.85 |
| 3 | **GoldBracket_EarlyLeader** | -9.32% | 16 | 16 | $11,137.79 |
| 4 | **MomTick_Micro** | -11.66% | 59 | 59 | $139,994.94 |
| 5 | **Crypto15M_EarlyLeader** | -58.58% | 43 | 43 | $91,645.77 |

> **ForecastEdge_Weather is the forward test.** It trades only where the point-in-time NWS archive (Austin (Camp Mabry): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Chicago (Midway): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Denver (DEN): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Los Angeles (LAX): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Miami (MIA): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; New York (Central Park): 10 snapshot(s), first 2026-09-18T03:19:26.166Z; Philadelphia (PHL): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Phoenix (PHX): 8 snapshot(s), first 2026-09-18T13:24:29.350Z; Seattle (Sea-Tac): 8 snapshot(s), first 2026-09-18T13:24:29.350Z) has a snapshot at or before the decision bar. On the backfilled August–September brackets it correctly abstains (0 trades, unranked, reason published); its real window is the live market from 2026-09-18 onward.
### The Live Desk — paper orders placed on real OPEN contracts, point-in-time

Cut-off **2026-09-19T11:08:18.437Z** (newest captured ladder). 63 of 69 tracked contracts had a real captured ladder at or before that instant; 26 orders were placed, 23 filled against real depth, 1 rested without crossing and was cancelled, and 0 position(s) were settled by the exchange's own result. Official fees: **$389.5129**. The desk audit (13 invariants, D1–D13) PASSES on this run: every fill is re-derived from the captured ladder it names, every fee from the official schedule with the series multiplier, every settlement from `settlement_value_dollars`.

| # | Desk username | Return | Equity | Fills | Contracts | Unfilled | Slippage cost | Fees | Settlement PnL |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | **LiveFavourite_Settle** | -0.00% | $99,999.42 | 2 | 374 | 0 | $0.3938 | $0.0000 | $0.00 |
| 2 | **LiveMeanRev_Spike** | -0.00% | $99,999.26 | 1 | 314 | 0 | $0.5853 | $0.0000 | $0.00 |
| 3 | **LiveNCAA_GameFavourite** | -0.01% | $99,992.53 | 2 | 908.58 | 33,809.42 | $0.0000 | $1.1162 | $0.00 |
| 4 | **LiveExpiryHarvest** | -0.01% | $99,989.50 | 2 | 1,750.63 | 30,184.37 | $0.0000 | $1.7509 | $0.00 |
| 5 | **LiveCheapBracket_Ladder** | -0.04% | $99,955.95 | 3 | 21,153 | 0 | $32.1759 | $0.0000 | $0.00 |
| 6 | **LiveFDA_DecisionPremium** | -0.05% | $99,949.90 | 3 | 1,219.6 | 1,409.4 | $19.3009 | $3.3776 | $0.00 |
| 7 | **LiveCEO_ChangeFav** | -0.22% | $99,782.66 | 2 | 6,504.47 | 12,163.53 | $109.9060 | $9.8691 | $0.00 |
| 8 | **LiveLongshot_Convexity** | -0.28% | $99,720.91 | 3 | 44,974.62 | 1,204,473.38 | $21.5986 | $32.6329 | $0.00 |
| 9 | **LiveTailPremium_NO** | -0.48% | $99,521.18 | 3 | 28,734.86 | 25,591.14 | $0.4923 | $141.2760 | $0.00 |
| 10 | **LiveMomentum_Bars** | -0.55% | $99,446.52 | 2 | 23,669.08 | 83,159.92 | $234.3486 | $199.4902 | $0.00 |
| — | **LiveDepthSweep_Taker** | *no fillable order* | $100,000.00 | 0 | 0 | 0 | $0.0000 | $0.0000 | $0.00 |
| — | **LiveMakerTouch** | *no fillable order* | $100,000.00 | 0 | 0 | 0 | $0.0000 | $0.0000 | $0.00 |
| — | **LiveNBA_GameFavourite** | *no fillable order* | $100,000.00 | 0 | 0 | 0 | $0.0000 | $0.0000 | $0.00 |

> A desk entry that placed no fillable order at this cut-off is shown as **no fillable order**, not as 0%: no price is invented, so a strategy that finds no real size simply does not trade.

The **−15h cut-off** (`2026-09-18T20:08:18.437Z`) is the one window that both trades and settles inside the captured data: 21 fills, 0 real settlement(s), $286.5162 in official fees — the $1.00/$0.00 payouts come from the exchange's own finalization, not from a mark.

Reproduce either run locally with `node scripts/run-live-desk.mjs` (add `--as-of=ISO` for an older cut-off); the desk is deterministic, and `scripts/run-live-desk.mjs` exits non-zero if any desk invariant fails.

<!-- AUTO:RESULTS-END -->

Each strategy also ships a generated post-mortem — *why it worked*, *why it drew down*, and a
verdict — written entirely from the replay's own numbers (`src/analysis.js`).

---

## The Live Desk — placing real trades on open contracts

The competition leaderboard replays **history**. The Live Desk is the separate section that does the
other half of the request: it **places paper orders on real, still-open Kalshi contracts**, at a
chosen instant, and then tracks every one of them with the same evidence standard. It is the
"simulate placing a real trade" surface, and it is deliberately built so that it *cannot* quote a
price the exchange never showed.

**What a desk trade is.**

1. **A point in time.** The desk runs at a cut-off (the newest captured ladder, or −6h / −12h /
   −15h / −24h / −48h / −96h / −240h). Every contract's **own published `close_time`** decides
   whether it was open then — the current `status`/`result` is deliberately not used, so a contract
   that has since been finalized is still tradeable *at an earlier cut-off* if it was open then.
2. **A real ladder, never a model.** An order can only be priced from an order-book ladder that was
   **captured at or before** the cut-off. A ladder captured later is listed but refused
   (`LOOK_AHEAD_LADDER`); that refusal is part of the audit. Buying YES consumes the real **NO**
   bids at `1 − no_bid`, because that is the counterparty the exchange actually has.
3. **Real size.** Fills walk the captured levels in price order; an order larger than the book fills
   what exists and reports the rest as **UNFILLED**. No session may take more than 10% of the
   contract's real liquidity base (the larger of its 24h volume and its open interest, falling back
   to lifetime volume) — the ladder is still the binding constraint in every fill.
4. **Official fees and real settlement.** Fees come from Kalshi's published quadratic schedule with
   the **series multiplier the exchange reports** (`fee_multiplier` / `fee_type` on the captured
   market, including the maker-fee series), and a held position is resolved by the exchange's own
   `result` / `settlement_value_dollars` / `settlement_ts` — $1.00 or $0.00, no settlement fee.

**Why the numbers are trustworthy.** `buildDeskReport()` is one function used by both the server
and the GitHub-Pages build, so the two can never disagree. `node scripts/run-live-desk.mjs` exits
non-zero unless all desk invariants hold; they are run again in the browser and published in the
tab (D1–D13, each with its official source). The desk does **not** transmit anything to Kalshi: it
is a paper simulator, and the disclosure says so on the tab itself.

**What is tracked for every placed trade** (`data/reports/live-desk-ledger.jsonl`, and the same
records behind the tab): the order (strategy, ticker, side, count, type, limit), the fill (price,
touch, slippage in ticks and dollars, the exact captured levels consumed, the liquidity cap that
applied, the official fee with its multiplier), the resting order and its queue position, the
cancellation, the mark, and the settlement with the exchange's result and timestamp. Every record
carries the **ladder URL + capture timestamp** and the **market object URL + capture timestamp** it
came from, so any line can be re-checked against the source by hand.

**The desk entrants** carry unique usernames like the roster, trade only to maximise return (no
stop-losses, no position caps — sizing is a fixed fraction of cash bounded by real depth), and are
ranked on the same $100,000 start. A desk entry that finds no real size at a cut-off is listed as
**no fillable order**, never as 0%. Those usernames are reserved for humans even when the stored
year predates the desk (`DESK_RESERVED_USERNAMES`, kept set-equal to the roster by test 104).

**Where the year keeps it.** Running the desk — `GET /api/live-desk` on the server, or the Live
Desk tab in the static build — calls `attachDeskSession()`. Compact per-entrant results,
explanations, and every FILL/SETTLE row land in the one-year store (`deskMemory` plus `tradeLog`
rows with `kind: 'desk'`). Re-running the same cut-off replaces those rows rather than duplicating
them. Open positions are **not** carried into the next cut-off: that remaining gap is the
multi-day book, not this attach.

---

## The roster

Every entry in this roster shares one mandate, given by the competition rules: **highest return only**. None
of them carries a stop-loss, a position cap or a volatility target; that is a deliberate property of
the competition, not an oversight. On the 268-period window the spread runs from +9.25% to −16.30%.

<!-- AUTO:ROSTER-START (regenerated by scripts/render-docs.js — do not edit) -->
| Username | Strategy | Category | Mandate | Risk management | Sizing |
| --- | --- | --- | --- | --- | --- |
| **AlphaApex_Momentum** | Maximum Convexity Momentum Chaser | Convexity Momentum | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **GammaWhale_Squeeze** | High-Gamma Volume-Spike Breakout | Gamma / Volume Shock | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 85% of cash |
| **ContrarianKing_100x** | Deep-OTM Longshot Convexity Hunter | Tail Convexity | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 90% of cash |
| **YieldVulture_Arb** | Late-Stage High-Probability Sweep | Expiry Harvest | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 95% of cash |
| **FedPivotSniper** | Macro Print Mean-Reversion Sniper | Macro / Event | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 80% of cash |
| **VolatilityArb_MM** | Two-Sided Spread Harvester (Market Maker) | Market Making | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **TrendRide_FullTilt** | Trend-Following Pyramider | Trend / Pyramiding | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **EventCatalyst_Max** | Range-Breakout Catalyst Frontrunner | Breakout / Catalyst | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **ThetaHarvest_ShortOTM** | Deep-OTM Decay Harvester (buys NO) | Theta / Short Convexity | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **FeeArb_PremiumBuyer** | Fee-Optimal Favourite Buyer | Fee Structure / Favourite | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **LongshotFader_FLB** | Favorite–Longshot Bias Fader | Behavioral / Bias Harvest | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **PanicDip_ShockTiming** | Shock-Timing Panic-Dip Buyer (maker ladder) | Shock / Mean Reversion (maker) | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 60% of cash |
| **PanicFade_HourlyVol** | Hourly Panic-Fade Volatility Reversion | Intraday Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 60% of cash |
| **BookWall_BidLadder** | Order-Book Wall Maker Ladder | Liquidity Provision | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **AdjacentStrike_Ladder** | Adjacent-Strike Cheap Bracket Ladder | Bracket Laddering | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 90% of cash |
| **ShockTiming_StopOut** | Shock-Timing with the Source's Stop-Out | Shock / Mean Reversion (stopped) | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 80% of cash |
| **PanicFade_T4_S100** | Panic Fade — the source's own best configuration | Intraday Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **PanicFadeDeep_T6_TP6** | Deep Panic Fade with a 6-cent Take-Profit | Intraday Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **MeanRev_CheapBand** | Cheap-Band Mean Reversion (enter ≤ 0.10, exit ≥ 0.50) | Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **TightScalp_Fixed5** | Tight Scalp — the family's best setting, promoted as a control | Spread Scalping | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **MakerFlip_SpreadHarvest** | Maker Flip — rest the bid, flip the fill to the offer | Spread Harvest | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **ShockTiming_ModerateFav** | Shock Timing — the source's favourite-bucket filter, as a measured control | Shock / Mean Reversion (control) | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 100% of cash |
| **WeatherLadder_CheapBands** | Cheap Weather-Bracket Ladder (settles for real) | Weather / Ladder | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 30% of cash |
| **ForecastEdge_Weather** | Point-in-Time Forecast-Divergence Buyer | Weather / Model vs Market | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 40% of cash |
| **GoldBracket_EarlyLeader** | 15-Minute Gold Early-Leader Ride | Gold / Short-Horizon Momentum | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **WeatherLadder_MultiCity** | Multi-City Cheap Weather-Bracket Ladder | Weather / Ladder (multi-city) | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **WeatherFavourite_Decay** | Weather Favourite Longshot-Fader (buys the likely band, sells the tail) | Weather / Favourite–Longshot | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 35% of cash |
| **SportsFavourite_Settle** | Game-Line Favourite, Held to Settlement | Sports / Favourite–Longshot | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **SportsUnderdog_Sweep** | Cheap-Underdog Convexity Sweep | Sports / Tail Convexity | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 20% of cash |
| **SportsLine_Momentum** | In-Play Line-Move Follower | Sports / Momentum | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 40% of cash |
| **SportsSteam_Fade** | Steam Fader (buys the side the market just dumped) | Sports / Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 30% of cash |
| **Crypto15M_EarlyLeader** | 15-Minute Crypto Early-Leader Ride (BTC · ETH · SOL) | Crypto / Short-Horizon Momentum | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **Crypto15M_CheapTail** | 15-Minute Crypto Cheap-Tail Buyer | Crypto / Tail Convexity | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 15% of cash |
| **FedBucket_Ladder** | Fed-Decision Bucket Ladder | Macro / Event Ladder | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 25% of cash |
| **CPI_PrintFade** | CPI-Print Panic Fade | Macro / Mean Reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 30% of cash |
| **FDALadder_Dominance** | FDA Cumulative-Ladder Dominance | FDA / No-Arbitrage Ladder | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 35% of cash |
| **CEOExit_Drift** | CEO-Exit News Drift | CEO / Event Drift | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **ForecastEdge_MultiCity** | Multi-City Point-in-Time Forecast Confirmation | Weather / Model vs Market (multi-city) | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 35% of cash |
| **MEE_BoardSum** | Mutually-Exclusive-Event Board Sum (R14 `mee`) | Cross-market / Board mean reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 40% of cash |
| **FadeSpike_Micro** | 5-Minute Spike Fade on 1-Minute Bars (R14 `fade`) | Short-Horizon / Mean reversion | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
| **MomTick_Micro** | 1-Minute Momentum Follow (R14 `mom`, adapted) | Short-Horizon / Momentum | MAXIMUM RETURN. No stop-losses, no position caps, no volatility targeting. | NONE (by mandate) | 50% of cash |
<!-- AUTO:ROSTER-END -->

---

## Run it

### Server mode (full features)

```bash
npm start                 # http://localhost:3000  (binds 0.0.0.0)
```

Nothing to install beyond Node 18+ — there are no dependencies. Optional environment variables:

| Variable | Default | Effect |
| --- | --- | --- |
| `PORT` | `3000` | HTTP port |
| `HOST` | `0.0.0.0` | Bind address |
| `KALSHI_API_KEY_ID` / `KALSHI_API_PRIVATE_KEY` | unset | Enables the **authenticated WebSocket relay**, so the feed carries real Kalshi ticks instead of modelled ones |
| `ALLOW_USER_CODE` | `false` | Server-side execution of user-submitted Strategy Lab code. Off by default; the Lab runs client-side either way |

```bash
npm run desk:data          # regenerate src/desk-data.js from data/history/ (real captured ladders)
npm run desk:run           # run the desk for every cut-off and write data/reports/live-desk*.json
npm run desk:run -- --as-of=2026-09-18T03:46:22.458Z   # run at an older cut-off
```

### Static mode (GitHub Pages)

`npm run build` copies `index.html` and `src/` into `docs/`. Opening `docs/index.html` — or the
Pages URL — runs the whole app in the browser: it detects that no server is present, imports the
same modules directly, computes the competition locally and stores your year in `localStorage`.
Every number is identical to server mode because it is the same code.

---

## The ten tabs

| Tab | What it shows |
| --- | --- |
| **Leaderboard** | Podium + full table, equity sparklines, per-strategy attribution bars, generated post-mortems, and the unranked block |
| **Strategies** | The whole roster: thesis, entry/sizing/exit rules, mandate, and computed results |
| **Markets & Depth** | Real captured markets, the reciprocal YES/NO ladder, and an order ticket that quotes fees and unfilled remainder *before* you commit |
| **1-Year Memory** | The 52-week calendar, market regimes, human registrations, the attached Live Desk session (one cut-off), and the full trade log — the dataset for future analysis |
| **Strategy Lab** | Write `decide(ctx)`, lint it, run it on the real candlesticks, and rank it against the roster |
| **Live Desk** | **Places paper orders on real OPEN Kalshi contracts** at a chosen point in time: an order ticket priced from the captured ladder, the desk leaderboard, the open-contract universe with its ladders, the order/fill/settlement log, and the desk audit's invariants |
| **Trade Ledger** | Every fill and round trip the competition engine booked, keyed to the real market period it traded in (never a run's wall clock), with the depth label and the source URL for each row |
| **Research** | Every public strategy this project read, what was taken from it, whether this repository's data can test it, and the computed facts/statistics behind that judgement |
| **Verification** | Every verified fact with its status and a clickable source |
| **Irregularities** | Everything that looked wrong in official data, with evidence and the action taken |

---

## Architecture

```
index.html            ten-tab shell
server.js             zero-dependency HTTP + WebSocket server, REST API, persistent store
build.js              copies the app into docs/ for GitHub Pages
scripts/render-docs.js regenerates VERIFICATION.md, IRREGULARITIES.md and README's AUTO blocks
scripts/ingest-history.mjs   appends each day's real candlesticks (+ depth) to data/history/
scripts/track-settlements.mjs polls the real status/result of every tracked market
scripts/sensitivity-sweep.mjs 270-run robustness matrix → SENSITIVITY.md
scripts/cap-comparison.mjs   the same roster under 4 per-market capital caps × 3 seeds
                             → data/reports/cap-comparison.json
scripts/generate-history-module.mjs  data/history/ → src/accumulated-history.js (browser-safe)

src/kalshi-config.js      verified constants: endpoints, fee schedule, 11 price-level structures,
                          rate limits, auth spec, candle periods, status enums, fixed-point rules
src/kalshi-fees.js        the official fee engine (taker/maker, per-tier, round-trip estimates)
src/price-grid.js         tick resolution, snapping and fixed-point string formatting
src/verified-snapshot.js  real captured markets, series, order books, candlesticks, 404 evidence
src/verified-candles.js   TWO 61-bar daily series (T33000, T19000) in compact form + an expander
                          checked field-for-field against two verbatim bars (one no-trade bar)
src/accumulated-history.js GENERATED from data/history/: every bar the ingest job has stored,
                          compactly encoded, with a verbatim oracle bar per market
src/history-merge.js      the rule that lets that store feed the replay: a stored series is used
                          only as a VERIFIED SUPERSET of a capture, never as a replacement
src/json-utils.js         stableStringify/stableEqual — deep comparison that actually compares
                          nested objects (the key-array replacer silently skipped them, #20)
src/market-analytics.js   multi-market accounting: per-market exposure, HHI concentration and
                          cross-market correlation with an honest "not computed" path
src/settlement-tracker.js real settlement tracking: books $1/$0 only on finalized + yes/no
src/history-store.js      reader for the accumulated data/history/ store (Node side)
src/kalshi-api.js         wire parsing, reciprocity checks, normalization, live/proxy/snapshot client
src/kalshi-auth.js        RSA-PSS request signing (Node only)
src/simulation-engine.js  OrderBook (real depth, partial fills, market making) + PaperPortfolio
src/backtest-replay.js    the deterministic replay engine and equity-curve analytics
src/strategies.js         the executable roster + self-validation
src/strategy-runner.js    competition runner (seeded, per-series fees, regime control)
src/analysis.js           attribution that reconciles, qualification rules, generated post-mortems
src/competition-memory.js the one-year store: calendar, participants, trade log, export/import
src/strategy-sandbox.js   lint + compile of user strategies, action sanitization
src/ws-lite.js            RFC 6455 frame codec and WebSocket server/client (no dependencies)
src/kalshi-ws.js          Kalshi subscribe/parse shapes, backoff, relay + upstream feed managers
src/verification-data.js  the verified facts, the irregularities register and the competition-site
                          analyses (live counts are in the AUTO block at the top of this file)
src/app.js                the browser controller (server mode + static mode)
src/ui.js / src/styles.css render helpers and the design system

test/simulation.test.js   the suite: fees vs the official table, grids, reciprocity, capture
                          integrity, execution honesty, roster integrity, replay determinism,
                          attribution reconciliation, multi-market accounting, correlation bounds,
                          settlement booking rules, ingest-merge integrity, accumulated-history
                          round trip and superset rule, volume-bounded fills, per-market caps,
                          sandbox, memory, WebSocket codec, audit trail
test/ui-smoke.mjs         runs the whole UI against a DOM stub and asserts what it rendered
```

---

## REST API

| Endpoint | Method | Purpose |
| --- | --- | --- |
| `/api/health` | GET | Service, version, credential state |
| `/api/state` | GET · POST | The full one-year memory (or replace it) |
| `/api/run-competition` | POST | Deterministic replay; `{seed, regime, settleAtEnd, finalResult, maxNotionalPerMarketPct, maxFillFractionOfPeriodVolume, flight}` — `flight` selects the store: `daily` (default, 30-market daily bars), `hourly` (60-minute bars, incl. the KXHIGHNY weather brackets), `micro` (1-minute bars, KXGOLD15M gold). Separate leaderboards, never merged. |
| `/api/leaderboard` | GET | Memory leaderboard + computed leaderboard + provenance |
| `/api/markets` | GET | Real captured markets |
| `/api/orderbook` | GET | Book for a ticker, with `depthModel` and mutation provenance |
| `/api/quote` | GET | Pre-trade quote: filled / unfilled / VWAP / slippage / official fee |
| `/api/trade` | POST | Execute a paper order for a registered participant |
| `/api/register` | POST | Register a human competitor (validated, collision-checked) |
| `/api/advance` | POST | Move the one-year calendar forward N weeks |
| `/api/settle` | POST | Resolve open positions at $1.00 / $0.00 |
| `/api/settlements` | GET | Real settlement state per market (`?source=live` polls the exchange) |
| `/api/market-stats` | GET | Multi-market exposure per strategy + cross-market correlation |
| `/api/history` | GET | The accumulated history store written by the daily ingest job |
| `/api/history-audit` | GET | What the store added, what the replay uses, and every market it refused with the reason |
| `/api/regimes` · `/api/regime` | GET · POST | List / apply a market-regime stress preset |
| `/api/backtest` | POST | Run user code server-side (disabled unless `ALLOW_USER_CODE=true`) |
| `/api/candlesticks` · `/api/verified` | GET | Captured candles / the audit ledger |
| `/api/export/json` · `/api/export/csv` | GET | The whole year as memory for future analysis |
| `/api/live-desk` | GET | The **Live Desk** report at `?asOf=&capital=`: coverage, desk leaderboard, explanations, the full order/fill/mark/settlement ledger, and the desk audit. Same builder the static build uses |
| `/api/live-desk/cutoffs` | GET | The cut-offs that have a captured ladder, with how many contracts each one can price |
| `/api/live-desk/universe` | GET | Every tracked contract with its real dates, touch, depth, fee multiplier and the ladder (or the stated reason it cannot be priced) |
| `/api/live-desk/ticket` | GET | Pre-trade preview priced from the captured ladder: fill, VWAP, slippage in ticks and $, official fee, unfilled remainder |
| `/api/live-desk/order` | POST | Record a paper order (`ticker`, `action`, `side`, `count`, `type`, `limit`, `strategy`, `asOf`) → order + fills, appended to `data/store/live-desk-orders.jsonl` |
| `/api/live-desk/orders` | GET · DELETE | The recorded paper orders / clear the log |
| `/api/live-desk/ledger.jsonl` · `/api/live-desk/fills.csv` | GET | The desk ledger as data-efficient NDJSON / a fill table for future analysis |
| `/api/kalshi/*` | GET | Server-side proxy to the real API, falling back to the capture |
| `/ws/feed` | WS | Ticker feed: real relay when credentials exist, otherwise labelled SIMULATED |

---

## Fee engine

Kalshi's published formula, implemented exactly and checked against Kalshi's own table:

```
taker: fees = round up(M × 0.07   × C × P × (1 − P))
maker: fees = round up(M × 0.0175 × C × P × (1 − P))
round up = "rounds up such that the fee + positionCost is rounded to a centicent"
```

Two verified facts drive the results:

- **`KXBTCY` has `fee_multiplier: 0`** — the BTC series is genuinely fee-free. Confirmed twice:
  the live `GET /series/KXBTCY` capture *and* the PDF's Non-Standard Fees table.
- **Fees are asymmetric by side.** Buying YES at $0.12 costs ≈ 6.2% of the premium in fees; buying
  NO at $0.88 costs ≈ 0.8%. That single asymmetry decides which strategies survive the window.

Source: <https://kalshi.com/docs/kalshi-fee-schedule.pdf> (effective 2026-07-07).

---

## Tests

```bash
npm test          # 58 assertion groups against official documents and real captures
npm run test:ui   # renders the entire UI against a DOM stub and checks the output
npm run test:all  # both
```

The suite treats official documents as oracles: the fee table (21 rows), the RFC 6455 handshake
worked example, the documented price-level structures, and two verbatim candlestick bars. It also
asserts the properties that keep the simulation honest — no invented fills, reconciling attribution,
deterministic replays, unranked non-traders, no payout before a market is `finalized`, and no
correlation claimed below the 20-period overlap floor.

Eight of the tests are new in this session and cover the work below: deep-comparison correctness
(#20), the T19000 capture's contiguity/no-trade handling and its deep agreement with the earlier
independent capture, multi-market exposure reconciliation, correlation bounds, settlement booking
rules, and the ingest merge's conflict detection.

---

## Command-line jobs

| Command | What it does | Needs network |
| --- | --- | --- |
| `node scripts/ingest-history.mjs` | Appends each day's **real** candlesticks (and optionally order-book snapshots) to `data/history/`. Incremental, idempotent, never overwrites a stored bar; a restated bar is recorded as a CONFLICT. `--days=0` (the default) backfills from each market's `open_time`. | yes |
| `node scripts/ingest-history.mjs --verify` | Audits the store offline: bar counts, gaps, no-trade periods. | no |
| `node scripts/track-settlements.mjs` | Polls `GET /markets/{ticker}` and reports each market's real `status` / `result`. Books a payout **only** on `finalized` + yes/no. | yes (use `--offline` to classify the captures) |
| `node scripts/sensitivity-sweep.mjs` | 10 strategies × 3 seeds × 3 universes × 3 settlement scenarios = 270 strategy runs. Writes `SENSITIVITY.md`. | no |
| `node scripts/cap-comparison.mjs` | The roster under 4 per-market capital caps × 3 seeds → `data/reports/cap-comparison.json`. | no |
| `node scripts/generate-history-module.mjs` | Compiles `data/history/` into `src/accumulated-history.js` so the browser build reads the same bars as the server. | no |
| `node scripts/generate-desk-module.mjs` | Compiles `data/history/` into `src/desk-data.js`: every tracked contract with its real dates, quotes and captured order-book ladders (levels stored as integer milli-dollars, with the capture timestamp and URL of each ladder). Deterministic, with the selection rule written into the module. | no |
| `node scripts/run-live-desk.mjs` | Runs the Live Desk at every cut-off (`--as-of=ISO` for one), places the desk entries' paper orders against the captured ladders, settles what the exchange finalized, and writes `data/reports/live-desk.json` + `live-desk-ledger.jsonl` + `live-desk-universe.json` + `live-desk-cutoffs.json`. **Exits non-zero if any desk invariant fails.** | no |

Daily automation: `.github/workflows/daily-history.yml` runs the ingest every day at 06:15 UTC on a
GitHub-hosted runner (which *can* reach `external-api.kalshi.com` — this sandbox cannot, see
Irregularity #4/#24) and commits only when new bars arrived. `.github/workflows/ingest-now.yml`
does the same on demand: commit a bumped `.github/triggers/ingest.json` and it runs on that branch,
because the automation token is not permitted to create `workflow_dispatch` events (HTTP 403).

---

## Multi-market accounting

The competition replays **30 markets at once** — 20 Nasdaq-100 yearly-range strikes, nine KXBTCY
strikes (the fee-free series) and four S&P 500 yearly-range strikes, 204–268 real daily bars each
(2025-12-24 → 2026-09-17). `src/market-analytics.js` accounts for that:

- **per-market exposure** — contracts traded, fees, realized P&L, contracts still open, capital at
  risk and each market's share of it;
- **concentration** — a Herfindahl–Hirschman index over capital at risk (1.00 = one market);
- **cross-market correlation** — Pearson ρ of daily close-to-close *dollar* changes on the real
  closes, computed only above a minimum overlap. Pairs beneath that floor are reported as
  **not computed** with the overlap they do have, rather than a number nobody can check.

---

## Strategy sources

The roster's entries are original designs built on the verified mechanics of the exchange (fees,
tick grids, reciprocal books, settlement), plus **recreated from published material** so they could
be forward-tested against real data rather than asserted — two from public trading write-ups
(`LongshotFader_FLB`, `PanicDip_ShockTiming`) and, since 2026-09-18, three from the owner's own
sibling project [PriceKalshiHistorical](https://github.com/buffedlizard55-lab/PriceKalshiHistorical)
(RESEARCH_SOURCES R14: `MEE_BoardSum`, `FadeSpike_Micro`, `MomTick_Micro` — the `mee` / `fade` / `mom`
reference strategies of its book-walking backtester, recreated on this store's real bars with every
granularity adaptation labelled on the entry):

| Username | Recreated from | What was taken |
| --- | --- | --- |
| **LongshotFader_FLB** | [Kalshi prediction market strategies — "Fade the Longshots" / "Buy Heavy Favorites"](https://laikalabs.ai/prediction-markets/kalshi-prediction-market-trading-strategies) | The 5c–15c longshot band and the 85c–95c favorite band as the two supposedly mispriced zones. |
| | [The Favorite-Longshot Bias: Evidence from Polymarket](https://pith.science/paper/2609.12878) | The caveat: longshots lose 6.3c/$ weighted per contract but **gain** 4.1c/$ when grouped by parent event — the sign is aggregation-dependent, so this is a hypothesis under test. |
| | [Favorite-longshot bias — overview](https://www.grokipedia.com/page/Favorite-longshot_bias) | Background on why high-price contracts resolve favourably more often than their price implies. |
| **PanicDip_ShockTiming** | [r/PredictionsMarkets: shock-timing bot build log](https://www.reddit.com/r/PredictionsMarkets/comments/1u3rn8s/i_built_a_39_kalshi_trading_bot_to_exploit_world/) | Rest limit buys below the pre-shock price at historical drop depths; exit with a resting offer 4–6c higher; keep both sides on resting orders to avoid taker fees. |
| | [OddsHopper — Kalshi trading strategy](https://www.oddsshopper.com/articles/prediction-markets/kalshi-trading-strategy) | "Rest limit orders instead of paying the spread" and "take profit by selling your position before settlement". |

Two honesty notes, both visible in the app:

- **The favorite leg of `LongshotFader_FLB` cannot fire on this universe.** Every captured contract
  trades between 1c and 28c, so no market ever reaches the 85c+ band. The leg is coded and will
  trade a future universe, but it contributes nothing to the current result — only the longshot leg
  is measured.
- **Neither source is exchange documentation.** They are third-party strategy write-ups, and the
  verification tab keeps them in a separate **Strategy sources** group so a forum post is never
  mistaken for a Kalshi spec. Test 46 asserts that only that group may cite a non-Kalshi host.

---

## Signal sources (the MasterSite review)

The owner's [master directory](https://buffedlizard55-lab.github.io/MasterSite/) of verified GitHub
Pages sites was reviewed site by site (2026-09-18) against one question: *could this project supply
a verified, point-in-time signal that a Kalshi strategy here could trade on?* The full ledger —
every entry linked for manual review, with testability verdicts — lives in `src/signal-sources.js`
and renders in the site's **Research** tab. Headline findings:

| Project | Verdict | What came of it |
| --- | --- | --- |
| **SFWeather** (NOAA/NWS pipeline) | live verified signal | The NWS point-forecast archive (`data/forecasts/`, ~5 captures/day) + the `KXHIGHNY` ingest + `ForecastEdge_Weather` / `WeatherLadder_CheapBands` |
| **PriceKalshiHistorical** (the owner's own Kalshi collector/backtester) | candidate → strategies recreated | Its three reference strategies (`mee`/`fade`/`mom`) are roster entries (R14): `MEE_BoardSum` (+1.18% hourly flight), `FadeSpike_Micro` (+76.57% micro flight), `MomTick_Micro` (−7.60%); its own 5s snapshot database is not exported, so only the rules were taken |
| **GOLD** | not a market signal (flagged) | The project is a *ring buyer's directory*; Kalshi's real gold markets (`KXGOLD15M`) are tracked directly instead, feeding `GoldBracket_EarlyLeader` |
| **PinePilot** (Pine Script lab) | testable archetype | Its TA families are already the roster's trend/momentum/mean-reversion entries, replayed with real fees (which the Pine simulator does not charge) |
| **DrugAnalysis (FDA) / NCAA football alerts / NBA Injury** | **mapped to the Live Desk** (Kalshi-price half) | Favourite-bucket idea now trades as desk entrants `LiveFDA_DecisionPremium` (→ `KXFDA*`), `LiveNCAA_GameFavourite` (→ `KXNCAAFGAME`) and `LiveNBA_GameFavourite` (→ `KXNBAGAME`) on real **open** contracts, priced from captured ladders. The injury/PDUFA/live-score *feeds* are still unarchived — S05/S06/S07 say so |
| **Insider-trades / NFL Injury / NFL & MLB scoreboards / SportsPred / TheLeap** | candidates | Real projects, real official sources — each blocked only by the matching point-in-time archive; the ledger states exactly what each would need. S02's remaining gap is Form 4, not Kalshi prices |
| **MLB-Prediction-model-backtest / MLB-PBP / PFFNFL / ScheduleFreeTime** (added by the 2026-09-18 second re-review) | candidates | Real projects on official sources (MLB statsapi, PFF public pages, league schedules) — each needs a point-in-time archive of its output before any honest test; S14–S17 state the exact blocker and the closing action |
| **NFLPRED / StockPaperSim** | not a signal (flagged) | NFLPRED is an empty stub (one README, verified); StockPaperSim is a *stock* competition on a different venue — and the directory's record for it is stale (Irregularity #42) |
| **CEO** | not found as a MasterSite repo (flagged) · **Kalshi CEO series mapped to the Live Desk** | No CEO repository exists among the account's public repos (Irregularity #32). The exchange *does* list CEO-exit series (`TESLACEOCHANGE`, `KXAAPLCEO`, …); those trade as desk entrant `LiveCEO_ChangeFav` on real open contracts. The KX-prefixed Tesla twin never traded (#43) |

---

## Limitations and remaining work

**Limitations of what exists today**

1. **The window is 268 periods, not 52 weeks.** Every market is backfilled to its own `open_time`,
   which for these series is 2025-12-23/24 — close to nine months. It is *not* a year, and it never
   will be from candlesticks alone: a market that opened last week cannot have a year of bars. The
   one-year competition calendar is therefore still a clock, and the oldest series in the universe
   sets how much of it can be measured.
2. **Depth behind the touch is captured, but re-anchored — not point-in-time.** The ladder is real
   (captured from the official orderbook endpoint), yet it is a single snapshot per market that is
   *re-anchored* to each bar's real quoted touch. A point-in-time ladder for every period would need
   streaming captures. Fills are also bounded by the bar's real traded volume (Irregularity #29), so
   the remaining exposure is the shape of the book between snapshots, not an invented one.
3. **Real settlements exist for the intraday universe and the settled daily markets.** The 60-minute
   store holds 199 markets of which 173 are finalized with the exchange's own result, and the
   1-minute store 43 of 43 (computed by `src/store-facts.js` on every load — these numbers grow
   with every ingest); their results settle for real ($1.00/$0.00, no fee). The still-active index
   markets of the daily flight remain `status: active, result: ""`, so positions there are marked
   at the last real quote, and `POST /api/settle` plus the sensitivity sweep's YES/NO rows stay
   labelled **hypothetical scenarios**. T33000 and T19000 close 2026-12-31; KXBTCY closes 2027-01-01.
4. **Point-in-time quotes.** Without API credentials the app never fetches live prices; with
   credentials only the WebSocket relay goes live (browser CORS + signed handshake). The relay has
   still never run against the live endpoint — no credentials existed in any session.
5. **Correlations are window-specific.** ρ is measured over one window on contracts that have not
   settled. It is a description of that window, not a forecast.
### The Live Desk — what it cannot do yet

- It prices only contracts with a **captured ladder**: 68 of 287 tracked markets
  on the 2026-09-18 capture (120 open contracts carry real quotes but no ladder
  and are listed as not priceable, each with the ingest command that fixes it).
- Cut-offs older than about −15h price nothing, because no ladder was captured
  before them yet; the scheduled 16:40 UTC books capture adds one per day.
- It attaches **one session per cut-off** into the one-year memory
  (`deskMemory` + `tradeLog` with `kind: 'desk'`). Re-running the same as-of
  replaces those rows. Open positions are **not** carried across days — that
  remaining gap is the multi-day book.
- It never transmits an order: a desk fill is a claim about the captured book,
  not about a real account.

6. **2,839 of the 10,905 stored bars (26.0%) are no-trade periods** — no OHLC at all, just the resting
   quotes and `previous_dollars`. On those bars the touch is carried forward, so a "fill" can rest on
   a stale price (Irregularity #30).
7. **The sandbox is defense-in-depth, not a security boundary.** `lintSource` is a token/pattern scan
   and user code still runs in your own browser (or your own server with `ALLOW_USER_CODE=true`).
8. **Single-process store.** The year lives in one JSON file under `data/store/` (git-ignored). No
   multi-user auth, no locking, no database.

**Suggested next work (in priority order — full detail in [ROADMAP.md](ROADMAP.md))**

1. **Let the forecast archive keep overlapping the live weather brackets, then measure.** The NWS
   archive began 2026-09-18T03:19Z (NYC) / 13:24Z (the other eight cities) and the bars now run
   through the same day — the overlap EXISTS and `ForecastEdge_Weather` / `ForecastEdge_MultiCity`
   replay it, but no forecast-vs-market divergence has met their entry conditions yet (0 fills,
   reason published). Every scheduled capture widens the honest forward window; re-run the reports
   and read the post-split numbers.
2. **True forward windows for the original roster.** `designedAt` is 2026-09-17/18, so bars from
   2026-09-19 onward are genuinely out-of-sample for every entry.
3. **A point-in-time signal archive for a sports or FDA project.** The Kalshi side is DONE (FDA,
   CEO and seven sports series ingested with settled results); the remaining gap is the owner's
   own projects (MLB model predictions, FDA PDUFA calendar, injury reports) archived with capture
   timestamps, the way `data/forecasts/` archives NWS.
4. **Point-in-time depth.** A second scheduled books capture now runs at 16:40 UTC each day
   (since 2026-09-18); the ladder behind the touch is still re-anchored between snapshots.
5. **Authenticate the relay and smoke-test it live.** Needs `KALSHI_API_KEY_ID` /
   `KALSHI_API_PRIVATE_KEY` and one live session to confirm.
6. **Headless browser test.** `test/ui-smoke.mjs` renders against a DOM stub; the browser download
   is not reachable from this sandbox.
7. **More strategies recreated from public write-ups.** Several exist already
   (`LongshotFader_FLB`, `PanicDip_ShockTiming`, `ShockTiming_StopOut`,
   `ShockTiming_ModerateFav`, `WeatherLadder_CheapBands` — plus `ForecastEdge_Weather` from the
   R06 weather-bot sweep, and the R14 trio `MEE_BoardSum` / `FadeSpike_Micro` / `MomTick_Micro`
   from the owner's PriceKalshiHistorical). Each new one needs a cited source in `Strategy sources`
   and its own forward test.
8. **A running multi-day desk book.** Desk fills now attach to the year (one
   session per asOf). What remains is carrying OPEN positions across cut-offs
   so a desk trader has a continuous year rather than a sequence of independent
   sessions.

### What the per-market capital-allocation experiment found

`scripts/cap-comparison.mjs` replays the roster under no cap, 50%, 25% and 10% of equity per market,
across three seeds (144 runs). With fills bounded by real traded volume, **the caps barely move the
result**: `ContrarianKing_100x` +9.34% uncapped vs +9.24% at a 10% cap, `VolatilityArb_MM` +9.25% vs
+9.82%, `GammaWhale_Squeeze` −14.84% vs −14.66%. Before the volume bound existed the same sweep
produced +2,005% and +146,402% — the concentration was being manufactured by modelled depth, not by
the strategies. Caps are an option (`maxNotionalPerMarketPct`), off by default, because the brief is
highest-return-only.

## Official sources

- Kalshi API documentation — <https://docs.kalshi.com/>
- Fee schedule (effective 2026-07-07) — <https://kalshi.com/docs/kalshi-fee-schedule.pdf>
- Order book response format — <https://docs.kalshi.com/getting_started/orderbook_responses>
- Fixed-point migration — <https://docs.kalshi.com/getting_started/fixed_point_migration>
- Candlesticks — <https://docs.kalshi.com/api-reference/market/get-market-candlesticks>
- WebSockets — <https://docs.kalshi.com/getting_started/quick_start_websockets>
- API keys & signing — <https://docs.kalshi.com/getting_started/api_keys>
- Rate limits — <https://docs.kalshi.com/getting_started/rate_limits>
- Historical data policy — <https://docs.kalshi.com/getting_started/historical_data>

Competition structure referenced (structure only, no data or branding taken):
[kalshi.com](https://kalshi.com/) ·
[TradingView The Leap](https://www.tradingview.com/the-leap/) ·
[Trade-Ideas Stock Trading Competition](https://www.trade-ideas.com/stock-trading-competition/) ·
[Candlecharts Contest](https://specials.candlecharts.com/contest/)

---

**Not affiliated with Kalshi.** This is an independent educational simulator. It places no real
orders, holds no funds, and every price it shows is either a captured Kalshi response or labelled as
modelled. Paper trading only.
